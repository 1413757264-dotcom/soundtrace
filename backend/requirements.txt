# 音迹 SoundTrace — Backend Dependencies
# Install: pip install -r requirements.txt

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Cache & Queue
redis>=5.0.0
celery>=5.3.0

# HTTP Client
httpx>=0.26.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# Config
pydantic-settings>=2.1.0

# Scraping
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Audio Pipeline
# demucs>=4.0.0  # Source separation (requires PyTorch — install separately)
# pyacoustid>=1.3.0  # Chromaprint fingerprinting
# chromaprint>=1.5.0

# Media Download
yt-dlp>=2024.0.0

# Search (optional — install when needed)
# elasticsearch>=8.0.0,<9.0.0

# Graph (optional — install when needed)
# neo4j>=5.0.0

# Storage
aiofiles>=23.0.0
boto3>=1.34.0  # S3 storage

# Monitoring
# sentry-sdk>=1.40.0
# prometheus-client>=0.19.0

# Dev
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0  # for TestClient
